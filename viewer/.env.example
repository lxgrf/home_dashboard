# Define the exact IP address and port of your main dashboard's web server.
DASHBOARD_URL=http://[IP_ADDRESS]/dashboard.png

# How often the Pi Zero should retrieve a new display image (in seconds). 600 = 10 minutes.
UPDATE_INTERVAL=600
